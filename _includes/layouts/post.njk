---
layout: layouts/base.njk
templateClass: o-post
---
<article class="c-post">
  {% include "partials/article-header.njk" %}

  <div class="u-flow c-article">
    {% include "partials/artwork-credit.njk" %}
    {{ content | safe }}
  </div>
</article>

<!-- Temporarily removed 'More like this' -->
<!-- <h2>More like this</h2> <ul> {%- if post.data.category == articles -%} {%- set nextPost = collections.articles | getNextCollectionItem(page) %} {%- if nextPost %} <li>Next: <a href="{{ nextPost.url | url }}">{{ nextPost.data.title }}</a> </li> {% endif %} {%- set previousPost = collections.articles | getPreviousCollectionItem(page) %} {%- if previousPost %} <li>Previous: <a href="{{ previousPost.url | url }}">{{ previousPost.data.title }}</a> </li> {% endif %} {% endif %} {%- if post.data.category == journal -%} {%- set nextPost = collections.journal | getNextCollectionItem(page) %} {%- if nextPost %} <li>Next: <a href="{{ nextPost.url | url }}">{{ nextPost.data.title }}</a> </li> {% endif %} {%- set previousPost = collections.journal | getPreviousCollectionItem(page) %} {%- if previousPost %} <li>Previous: <a href="{{ previousPost.url | url }}">{{ previousPost.data.title }}</a> </li> {% endif %} {% endif %} </ul> -->
